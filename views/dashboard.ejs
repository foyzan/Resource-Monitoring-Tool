<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js"></script>
    <style>
        :root {
            --primary-color: #007bff;
            --secondary-color: #28a745;
            --bg-light: #f8f9fa;
            --card-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.1);
        }
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; margin: 0; padding: 0; background-color: var(--bg-light); color: #343a40; text-align: center; }
        header { background-color: var(--primary-color); color: white; padding: 1.5rem 0; box-shadow: var(--card-shadow); margin-bottom: 2rem; }
        .dashboard-container { display: flex; flex-wrap: wrap; justify-content: center; gap: 2rem; padding: 0 1rem; }
        .metric-card { background-color: white; border-radius: 10px; padding: 20px; box-shadow: var(--card-shadow); transition: transform 0.2s; min-width: 300px; max-width: 380px; }
        .metric-card:hover { transform: translateY(-5px); }
        h2 { border-bottom: 2px solid #eee; padding-bottom: 10px; margin-top: 0; color: var(--primary-color); }
        /* Uptime style */
        #uptime-display { font-size: 2.5rem; font-weight: 700; color: var(--secondary-color); margin-bottom: 2rem; display: block; }
        .chart-container { height: 300px; width: 100%; }
    </style>
</head>
<body>
    <header>
        <h1><%= pageTitle %></h1>
    </header>

    <span id="uptime-display"><%= metrics.uptime_formatted %></span>
    
    <div class="dashboard-container">
        <div class="metric-card">
            <h2>CPU Utilization (%)</h2>
            <div class="chart-container">
                <canvas id="cpuChart"></canvas>
            </div>
        </div>

        <div class="metric-card">
            <h2>Process Memory (RSS in MB)</h2>
            <div class="chart-container">
                <canvas id="memoryRssChart"></canvas>
            </div>
        </div>

        <div class="metric-card">
            <h2>JS Heap Used</h2>
            <p style="font-size: 2rem; color: #dc3545;" id="heap-used-value"><%= metrics.memory_heap_used_mb %> MB</p>
            <small>Memory used by active JavaScript objects.</small>
        </div>
    </div>

    <script src="dashboard.js"></script> 
</body>
</html>